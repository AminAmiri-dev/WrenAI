# راهنمای مشارکت در داده یار

از مشارکت شما در داده یار سپاسگزاریم. این سند مسیر توسعه، فارسی‌سازی، review و دیپلوی را برای نسخه اختصاصی شرکت توسعه زیست بوم دیجیتال توضیح می‌دهد.

## اصول توسعه

- تغییرات باید کوچک، قابل بررسی و مرتبط با یک هدف مشخص باشند.
- رفتار موجود پروژه را بدون دلیل فنی تغییر ندهید.
- برای متن‌های قابل مشاهده کاربر از فارسی روان، رسمی و کوتاه استفاده کنید.
- در رابط کاربری، راست‌به‌چپ، فاصله آیکون‌ها، اندازه فیلدها و خوانایی متن‌ها را بررسی کنید.
- کلیدهای API، رمزها، فایل‌های `.env` واقعی و اطلاعات سرور را commit نکنید.
- اگر تغییر شما روی دیپلوی اثر می‌گذارد، GitHub Actions و فایل‌های Docker را هم بررسی کنید.

## شاخه‌ها و commit

شاخه اصلی دیپلوی این نسخه `legacy/v1` است. تغییرات آماده انتشار باید به این شاخه push شوند تا فرآیند build و deploy خودکار اجرا شود.

برای پیام commit از جمله کوتاه و روشن استفاده کنید:

```text
Rebrand repository documentation
Improve Persian home prompt layout
Fix private repository deploy fetch
```

## اجرای پروژه برای توسعه

پیش‌نیازهای معمول شامل Docker، Node.js، pnpm و ابزارهای مورد نیاز سرویس‌های backend است. ساده‌ترین راه اجرای کامل پروژه، Docker Compose است:

```bash
cd docker
cp .env.example .env
cp config.example.yaml config.yaml
docker compose --env-file .env up -d
```

در صورت توسعه فقط روی رابط کاربری، تنظیمات محلی همان package باید با نسخه‌های ثبت‌شده در پروژه هماهنگ باشد.

## فارسی‌سازی و تجربه کاربری

هنگام تغییر UI این موارد باید رعایت شوند:

- نام محصول در متن‌های قابل مشاهده باید «داده یار» باشد.
- متن‌های انگلیسی کاربرمحور باید ترجمه شوند.
- فونت پروژه باید با فونت فارسی تعریف‌شده در assets هماهنگ باشد.
- آیکون‌ها در حالت راست‌به‌چپ نباید به متن بچسبند.
- منو، هدر، فرم‌ها، جدول‌ها، modalها و پیام‌های خطا باید در اندازه‌های مختلف صفحه بررسی شوند.
- اگر بخشی مانند schema، SQL، نام ستون یا نام جدول ماهیت فنی چپ‌به‌راست دارد، چیدمان همان بخش می‌تواند چپ‌به‌راست بماند ولی برچسب‌ها و توضیحات باید فارسی باشند.

## تست و بررسی

پیش از ارسال تغییرات:

- build یا lint مربوط به بخشی که تغییر کرده اجرا شود.
- صفحه یا workflow تغییرکرده در مرورگر بررسی شود.
- برای تغییرات دیپلوی، سلامت سرویس‌ها روی سرور کنترل شود.
- برای تغییرات متنی، جست‌وجوی عبارات انگلیسی باقی‌مانده انجام شود.
- برای تغییرات امنیتی، نبودن secret در diff بررسی شود.

## دیپلوی

با push به شاخه `legacy/v1`، GitHub Actions تصویرهای Docker را می‌سازد، نسخه زمانی ثبت می‌کند، سرور را از طریق SSH به‌روزرسانی می‌کند و سلامت HTTP/HTTPS را بررسی می‌کند.

اگر مخزن private باشد، workflow از token خود GitHub Actions برای دریافت سورس روی سرور استفاده می‌کند؛ بنابراین نباید token دستی داخل سورس یا فایل‌های سرور ذخیره شود.

## گزارش مشکل

برای خطاهای معمولی، issue یا task داخلی ثبت کنید و مراحل بازتولید، تصویر خطا، URL صفحه و commit مربوط را بنویسید. برای آسیب‌پذیری امنیتی از راهنمای [SECURITY.md](./SECURITY.md) استفاده کنید.
